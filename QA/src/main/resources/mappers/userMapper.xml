<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.project.mapper.UserMapper">
	
	<!-- 테스트로 한번 해봄 -->
	
	<select id="getTime" resultType="string">
		select now()
	</select>
	
	<insert id="insertUser">
		insert
			into
		user
			(userId, password, userName, phoneNum, regDate, updateDate)
		values
			(#{userId}, #{password}, #{userName}, #{phoneNum}, now(), now())
	</insert>
	
	<select id="selectUser" resultType="com.project.qa.domain.UserVO">
		select 
			*
		from
			user
		where id = #{id}
	</select>  
	
	<select id="selectWithPW" resultType="com.project.qa.domain.UserVO">
		select 
			*
		from
			user
		where userId = #{userId} and password = #{password}
	</select>  
	
  </mapper>